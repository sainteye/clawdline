{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "images.schema.json",
  "title": "Pictures",
  "description": "GET /v1/artifacts/images/{id}, POST /v1/artifacts/images, and the `artifacts` a transcript entry carries. A picture crosses the wire as an opaque id and its measurements; its bytes are read from the id, and no path or file name ever travels.",
  "$defs": {
    "ImageAbsence": {
      "description": "Why a reference has no picture behind it: `expired` when the store held it and no longer does, `unknown` when no store here ever held that id.",
      "type": "string",
      "enum": ["expired", "unknown"]
    },
    "ImageArtifact": {
      "description": "One picture a transcript entry shows, in the Swift app's `SessionImageArtifact.object` shape. A reference with `state` describes no bytes: its size fields are 0 and `expires_at` is 1, which is the branch every renderer already draws as gone.",
      "type": "object",
      "additionalProperties": false,
      "required": ["byte_count", "expires_at", "height", "id", "media_type", "width"],
      "properties": {
        "byte_count": { "type": "integer" },
        "expires_at": { "description": "Unix seconds after which the picture is gone.", "type": "integer" },
        "height": { "type": "integer" },
        "id": { "description": "A lowercase UUID. The whole of what the bytes are fetched by.", "type": "string" },
        "media_type": { "description": "image/jpeg for a photograph, image/png for anything else: every stored picture was drawn and written out again.", "type": "string" },
        "state": { "$ref": "#/$defs/ImageAbsence" },
        "width": { "type": "integer" }
      }
    },
    "ImagePath": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path"],
      "properties": {
        "path": { "description": "One normalized absolute path to a regular local file. A link is refused.", "type": "string" }
      }
    },
    "ImageStoreRequest": {
      "description": "POST /v1/artifacts/images, with this machine's orchestrator token only: 1…6 local pictures a session wants to show in its own reply. Each is decoded, bounded and stored as PNG before any is kept.",
      "type": "object",
      "additionalProperties": false,
      "required": ["images"],
      "properties": {
        "images": { "type": "array", "items": { "$ref": "#/$defs/ImagePath" } }
      }
    },
    "StoredImage": {
      "description": "One stored picture and the literal to paste into a reply to show it: `<clawdline-image id=\"…\">`.",
      "type": "object",
      "additionalProperties": false,
      "required": ["byte_count", "expires_at", "height", "id", "marker", "media_type", "width"],
      "properties": {
        "byte_count": { "type": "integer" },
        "expires_at": { "type": "integer" },
        "height": { "type": "integer" },
        "id": { "type": "string" },
        "marker": { "type": "string" },
        "media_type": { "type": "string" },
        "width": { "type": "integer" }
      }
    },
    "ImageStoreResult": {
      "type": "object",
      "additionalProperties": false,
      "required": ["artifacts", "at", "ok"],
      "properties": {
        "artifacts": { "type": "array", "items": { "$ref": "#/$defs/StoredImage" } },
        "at": { "type": "integer" },
        "ok": { "type": "boolean" }
      }
    }
  }
}
