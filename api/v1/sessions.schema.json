{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "sessions.schema.json",
  "title": "The fleet list",
  "description": "GET /v1/sessions, and the `sessions` frame of GET /v1/events. They are the same payload because they are the same snapshot; two builders would drift and a client could not tell which one it got.",
  "$defs": {
    "WorkState": {
      "description": "One projection of what this session needs, ranked so a reader can sort by it. `unknown` outranks nothing and means the inputs were incomplete.",
      "type": "string",
      "enum": ["ready", "working", "holding", "waiting_you", "waiting_session", "unknown", "milestone_complete", "work_complete"]
    },
    "WorkProvenance": {
      "description": "Who decided a row's work state. `broker` means this daemon projected it from what it could read; `self` means the session said so. Only `broker` is produced here, because nothing yet takes a session's own word for what it needs — and emitting `self` where that is untrue would make a projection look like testimony.",
      "type": "string",
      "enum": ["broker", "self"]
    },
    "CloseabilityState": {
      "description": "`safe` is a positive claim that nothing is owed. An unreadable obligation list must produce `unknown`, never `safe`.",
      "type": "string",
      "enum": ["safe", "blocked", "needs_attestation", "unknown"]
    },
    "CloseMover": {
      "description": "Who clears the thing standing in the way. `self` distinguishes the session that has to act from another one, which is the difference between a thing a reader can do now and a thing they can only wait for.",
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "person_needed", "self"],
      "properties": {
        "kind": { "type": "string", "enum": ["session", "person", "task", "broker"] },
        "person_needed": { "type": "boolean" },
        "self": { "type": "boolean" }
      }
    },
    "CloseReason": {
      "description": "One thing in the way. It names the machine code, the subject it is about and who moves it: a code alone says there is a problem but not which object has it, and a sentence alone cannot be grepped for in a log.",
      "type": "object",
      "additionalProperties": false,
      "required": ["code", "kind", "mover", "subject_id", "subject_kind"],
      "properties": {
        "code": { "type": "string" },
        "kind": { "type": "string", "enum": ["obligation", "evidence", "attestation"] },
        "mover": { "$ref": "#/$defs/CloseMover" },
        "subject_id": { "type": "string" },
        "subject_kind": { "type": "string" }
      }
    },
    "CloseSource": {
      "description": "Where the reading came from and how old it is. `freshness` is what stops a stale `safe` being read as a current one — the console refuses to call anything safe unless this says `current`.",
      "type": "object",
      "additionalProperties": false,
      "required": ["freshness", "max_age_seconds", "observed_at", "provenance"],
      "properties": {
        "freshness": { "type": "string", "enum": ["current", "stale"] },
        "max_age_seconds": { "type": "integer" },
        "observed_at": { "type": "integer" },
        "provenance": { "type": "string" }
      }
    },
    "Closeability": {
      "description": "Whether this session can end. A separate question from whether it can take work, and neither may be read off the other.\n\n`safe` is a positive claim and the reader enforces what it costs: no reasons, a current source, an attestation id and a version, and a session that is not working, waiting or unreadable. Anything short of that reads as `unknown`, which is why an unreadable obligation list can never come out as safe.",
      "type": "object",
      "additionalProperties": false,
      "required": ["mover", "observed_at", "provenance", "reasons", "source", "state", "version"],
      "properties": {
        "activity_generation": { "description": "Absent here. The Swift app counts activity for its own caching and this daemon has no such counter; a number invented to fill the field would be a counter that never moves, which is worse than a field that is not there. Nothing in the console's projection reads it.", "type": "integer" },
        "attestation_id": { "description": "The identifier of the reading that proves nothing is owed. Null until something attests, and a null here is exactly why a state that would otherwise be safe reads as unknown.", "nullable": true, "type": "string" },
        "mover": { "$ref": "#/$defs/CloseMover" },
        "obligation_generation": { "description": "Absent here, for the same reason as `activity_generation`.", "type": "integer" },
        "observed_at": { "type": "integer" },
        "provenance": { "type": "array", "items": { "type": "string" } },
        "reasons": { "type": "array", "items": { "$ref": "#/$defs/CloseReason" } },
        "session_generation": { "description": "Which reading of the machine this came from — the same counter the snapshot's scan carries, so a closeability and the list it arrived with can be told apart from a later pair.", "type": "integer" },
        "source": { "$ref": "#/$defs/CloseSource" },
        "state": { "$ref": "#/$defs/CloseabilityState" },
        "version": { "description": "Which computation produced this. A reading with no version is a reading nobody can say the rules for, and the console treats it as unproven.", "type": "string" }
      }
    },
    "SessionRow": {
      "description": "One assistant session. Fields this daemon cannot support are absent rather than invented; a reader that handles absence handles this too.",
      "type": "object",
      "additionalProperties": false,
      "required": ["backend", "closeability", "evidence", "id", "isClaude", "state", "work_state"],
      "properties": {
        "assistant": { "$ref": "common.schema.json#/$defs/Assistant" },
        "backend": { "$ref": "common.schema.json#/$defs/Backend" },
        "closeability": { "$ref": "#/$defs/Closeability" },
        "cwd": { "type": "string" },
        "evidence": { "$ref": "common.schema.json#/$defs/Evidence" },
        "icon": { "$ref": "common.schema.json#/$defs/Icon" },
        "id": { "type": "string" },
        "isClaude": { "type": "boolean" },
        "label": { "description": "What the session is called: a name typed in Clawdline, the title of the task Clawdline opened it for, the conversation's own title (`/rename`, else `aiTitle`), Codex's thread name, Claude Code's registry handle — the first that has something. Never a terminal title. This daemon keeps no typed names and no task titles, so its rows start at the conversation's own title. Absent when nothing names the session.", "type": "string" },
        "line": { "description": "What a working session says it is doing, read from its screen with the assistant's own clock in it. Present only while working. The Swift app sends it under this name and the row's height depends on it.", "type": "string" },
        "sessionId": { "description": "The assistant's own conversation id, when one was recovered from its command line.", "type": "string" },
        "shells": { "description": "The commands this session left running in the background, newest first, at most six. Absent rather than empty when there are none, as the Swift app sends it; always absent for Codex, which keeps no record of them.", "type": "array", "items": { "$ref": "#/$defs/SessionShell" } },
        "state": { "$ref": "common.schema.json#/$defs/SessionState" },
        "tty": { "type": "string" },
        "work_provenance": { "$ref": "#/$defs/WorkProvenance" },
        "work_state": { "$ref": "#/$defs/WorkState" }
      }
    },
    "SessionShell": {
      "description": "One background command a session started and has not finished: its output file has no ending written under it, and the session's transcript announced its id as a background command. `at` is when it last printed something.",
      "type": "object",
      "additionalProperties": false,
      "required": ["at", "id"],
      "properties": {
        "at": { "description": "Unix seconds.", "type": "integer" },
        "command": { "description": "The command line that started it, on one line. Absent when the two transcript records it is joined from straddled a read.", "type": "string" },
        "doing": { "description": "The last line it printed, clipped to 160 characters.", "type": "string" },
        "id": { "description": "Claude Code's own id for it.", "type": "string" },
        "what": { "description": "The description written beside the command, when there was one.", "type": "string" }
      }
    },
    "SessionModel": {
      "description": "One model this session's assistant can be switched to. A session's current model is matched against `id` by prefix, so a dated id still finds its row; `name` is what a reader is shown for it.",
      "type": "object",
      "additionalProperties": false,
      "required": ["command", "id", "name"],
      "properties": {
        "command": { "type": "string" },
        "id": { "type": "string" },
        "name": { "type": "string" }
      }
    },
    "SessionInfoSession": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id"],
      "properties": {
        "assistant": { "$ref": "common.schema.json#/$defs/Assistant" },
        "cwd": { "type": "string" },
        "id": { "type": "string" },
        "model": { "description": "The model the session is on, as its record last named it: the newest assistant turn's model, or a `/model` switch made after it. Absent when the record says nothing.", "type": "string" },
        "sessionId": { "type": "string" },
        "title": { "description": "The same label the session list shows.", "type": "string" }
      }
    },
    "SessionInfoUsage": {
      "description": "What the conversation has spent. For Claude it is the sum of every assistant turn in the transcript and is absent when the transcript is larger than 8 MiB, because a partial sum is not a total. For Codex it is Codex's own running total.",
      "type": "object",
      "additionalProperties": false,
      "required": ["cacheRead", "cacheWrite", "input", "output", "total"],
      "properties": {
        "cacheRead": { "type": "integer" },
        "cacheWrite": { "type": "integer" },
        "costUsd": { "description": "US dollars. Claude Code's own session total from its status-line cache when it wrote one, otherwise these tokens at list price. Absent when neither is known, which is always the case for Codex; also absent at exactly zero.", "type": "number" },
        "input": { "type": "integer" },
        "model": { "description": "The model the last counted turn named, as written.", "type": "string" },
        "output": { "type": "integer" },
        "total": { "type": "integer" }
      }
    },
    "SessionInfo": {
      "description": "The facts behind the status line under an open session. This daemon serves the transcript-derived part the Swift app calls the summary; the working tree, context use, plan windows, links, permission and fast mode are not read here and their keys are absent.",
      "type": "object",
      "additionalProperties": false,
      "required": ["models", "session"],
      "properties": {
        "models": { "type": "array", "items": { "$ref": "#/$defs/SessionModel" } },
        "session": { "$ref": "#/$defs/SessionInfoSession" },
        "usage": { "$ref": "#/$defs/SessionInfoUsage" }
      }
    },
    "SessionInfoReply": {
      "description": "GET /v1/sessions/{id}/info. Kept off the session stream because answering reads the session's record; the console holds an answer for a minute.",
      "type": "object",
      "additionalProperties": false,
      "required": ["info"],
      "properties": {
        "info": { "$ref": "#/$defs/SessionInfo" }
      }
    },
    "ScanCompleted": {
      "type": "object",
      "additionalProperties": false,
      "required": ["complete", "sequence"],
      "properties": {
        "complete": { "type": "boolean" },
        "sequence": { "type": "integer" }
      }
    },
    "Scan": {
      "description": "What the reading behind this snapshot knows about itself. Without it an empty list cannot be told apart from a failed scan.",
      "type": "object",
      "additionalProperties": false,
      "required": ["complete", "completed", "emptyAuthoritative", "epoch", "generation", "provenance"],
      "properties": {
        "complete": { "type": "boolean" },
        "completed": { "$ref": "#/$defs/ScanCompleted" },
        "emptyAuthoritative": {
          "description": "An empty list is only authoritative when the reading was complete. This is what stops a client reading a failed scan as `every session went away`.",
          "type": "boolean"
        },
        "epoch": { "description": "Identifies the process. A client reconnecting to a restarted daemon uses it to tell a reset generation from one that went backwards.", "type": "integer" },
        "generation": { "type": "integer" },
        "provenance": { "type": "string" }
      }
    },
    "SessionsSnapshot": {
      "type": "object",
      "additionalProperties": false,
      "required": ["at", "scan", "sessions"],
      "properties": {
        "at": { "type": "integer" },
        "scan": { "$ref": "#/$defs/Scan" },
        "sessions": { "type": "array", "items": { "$ref": "#/$defs/SessionRow" } }
      }
    }
  }
}
