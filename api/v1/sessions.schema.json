{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "sessions.schema.json",
  "title": "The fleet list",
  "description": "GET /v1/sessions, and the `sessions` frame of GET /v1/events. They are the same payload because they are the same snapshot; two builders would drift and a client could not tell which one it got.",
  "$defs": {
    "WorkState": {
      "description": "One projection of what this session needs, ranked so a reader can sort by it. `unknown` outranks nothing and means the inputs were incomplete.",
      "type": "string",
      "enum": ["ready", "working", "holding", "waiting_you", "waiting_session", "unknown", "milestone_complete", "work_complete"]
    },
    "WorkProvenance": {
      "description": "Who decided a row's work state. `broker` means it was projected from what could be read; `self` means the session's own declaration (the Swift store's `session_self_states`) is what decided it.",
      "type": "string",
      "enum": ["broker", "self"]
    },
    "CloseabilityState": {
      "description": "`safe` is a positive claim that nothing is owed. An unreadable obligation list must produce `unknown`, never `safe`.",
      "type": "string",
      "enum": ["safe", "blocked", "needs_attestation", "unknown"]
    },
    "CloseMover": {
      "description": "Who clears the thing standing in the way, as the Swift app's CloseabilityMover.wire spells it. `self` distinguishes the session that has to act from another one, which is the difference between a thing a reader can do now and a thing they can only wait for. `self` is always present here; the Swift app leaves it out for a person, a task and the broker, where it would be false.",
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "person_needed", "self"],
      "properties": {
        "kind": { "type": "string", "enum": ["session", "person", "task", "broker"] },
        "person_needed": { "type": "boolean" },
        "self": { "type": "boolean" },
        "session_id": { "description": "The other session that moves it, when `kind` is `session` and `self` is false.", "type": "string" },
        "task_id": { "description": "The task that moves it, when `kind` is `task`.", "type": "string" }
      }
    },
    "CloseReason": {
      "description": "One thing in the way. It names the machine code, the subject it is about and who moves it: a code alone says there is a problem but not which object has it, and a sentence alone cannot be grepped for in a log. `subject_kind` and `subject_id` are absent for the evidence reasons that are about the reading rather than an object.",
      "type": "object",
      "additionalProperties": false,
      "required": ["code", "kind", "mover"],
      "properties": {
        "code": { "type": "string" },
        "kind": { "type": "string", "enum": ["obligation", "evidence", "attestation"] },
        "mover": { "$ref": "#/$defs/CloseMover" },
        "subject_id": { "type": "string" },
        "subject_kind": { "type": "string" }
      }
    },
    "CloseSource": {
      "description": "Where the reading came from and how old it is. `provenance` is `session_watch`, the scan of the machine's sessions this row came from. `freshness` is what stops a stale `safe` being read as a current one — the console refuses to call anything safe unless this says `current`.",
      "type": "object",
      "additionalProperties": false,
      "required": ["freshness", "max_age_seconds", "observed_at", "provenance"],
      "properties": {
        "freshness": { "type": "string", "enum": ["current", "stale", "missing"] },
        "max_age_seconds": { "type": "integer" },
        "observed_at": { "type": "integer" },
        "provenance": { "type": "string" }
      }
    },
    "Closeability": {
      "description": "Whether this session can end, by the Swift app's rules (Orchestrator.projectCloseability, CloseabilityIndex.swift) over the Swift app's store, read-only, plus this daemon's own obligations. A separate question from whether it can take work, and neither may be read off the other.\n\n`safe` is a positive claim and the reader enforces what it costs: no reasons, a current source, an attestation id and a version, and a session that is not working, waiting or unreadable. Anything short of that reads as `unknown`. An unreadable store is an `evidence` reason (`swift_store_unreadable`), so it can never come out as safe or as an empty list. `attestation_id` and `mover` are always present and may be null.",
      "type": "object",
      "additionalProperties": false,
      "required": ["activity_generation", "obligation_generation", "observed_at", "provenance", "reasons", "session_generation", "source", "state", "version"],
      "properties": {
        "activity_generation": { "description": "This terminal's turn clock from the Swift store (`session_activity`); 0 when it has none. An attestation names it, so a turn started after attesting invalidates the claim.", "type": "integer" },
        "attestation_id": { "description": "The identifier of the reading that proves nothing is owed. Null until something attests, and a null here is exactly why a state that would otherwise be safe reads as unknown.", "nullable": true, "type": "string" },
        "mover": { "description": "The one mover every reason points at; null when they point at more than one, which is itself the answer.", "$ref": "#/$defs/CloseMover", "nullable": true },
        "obligation_generation": { "description": "The Swift store's machine-wide obligation clock.", "type": "integer" },
        "observed_at": { "type": "integer" },
        "provenance": { "type": "array", "items": { "type": "string" }, "description": "`broker`, and `self` beside it when the session's own attestation matched this process." },
        "reasons": { "type": "array", "items": { "$ref": "#/$defs/CloseReason" } },
        "session_generation": { "description": "Which reading of the machine this came from — the same counter the snapshot's scan carries, so a closeability and the list it arrived with can be told apart from a later pair.", "type": "integer" },
        "source": { "$ref": "#/$defs/CloseSource" },
        "state": { "$ref": "#/$defs/CloseabilityState" },
        "version": { "description": "`cl1_` and 32 hex digits: the Swift app's closeabilityVersion over the exact process identity, the two clocks and the state. A reading with no version is a reading nobody can say the rules for.", "type": "string" }
      }
    },
    "IdentityBinding": {
      "description": "How a running process was tied to the conversation it is having, or — when `sessionId` is absent — which kind of nothing stands in the way. `command_line` is a resumed session, whose id is on its own command line; `open_file` is the transcript the process holds open, which the kernel names rather than a correlation between two clocks; `registry` is the assistant's own live record, which only Claude Code writes. The three that carry no id are deliberately not one word: `no_record` is a session that has written nothing to match yet — Codex writes its rollout at the first message and not at startup, so a session nobody has typed into is this, and names itself as soon as somebody does; `unreadable` is this machine failing to read the table of open files, which is its own fault to fix; `ambiguous` is transcripts belonging to more than one conversation open on the same process, where naming the session at all would be a guess \u2014 it is the conversations that are counted and not the files, because a Codex session running sub-agents holds one transcript per thread open and every one of them names the same conversation at its head. Absent on a row no identity source was asked about.",
      "type": "string",
      "enum": ["command_line", "open_file", "registry", "no_record", "unreadable", "ambiguous"]
    },
    "ActivityUnknownReason": {
      "description": "Which kind of nothing took the place of a session's activity time. They are deliberately not one word, for the same reason `IdentityBinding`'s three are not: `no_record` is a session that has written no conversation record yet — which is a session opened a moment ago, never one that has been quiet, because both assistants write that file at the first turn; `unreadable` is this machine failing to read a record that should be there, which is its own fault to fix; `unread` is a row this reading of the machine did not get to, having already read as many records as its bound allows (the capacity register's `sessions.activity_reads`); `unsupported` is a row nothing here can answer for at all. None of them may be drawn as an age or ordered as an old time.",
      "type": "string",
      "enum": ["no_record", "unreadable", "unread", "unsupported"]
    },
    "SessionActivity": {
      "description": "When this session last moved, from this daemon rather than from whatever each browser happened to have watched: the moment the session's own conversation record last grew. That one file follows both halves of a turn — the assistant appends its output to it and the person's message is appended to it too — so it is the single answer to a question that otherwise splits into three that disagree. State changes are not it: a session working since midnight changes state twice a night, and ordering by that would sink it below one that blinked an hour ago. The list orders by this inside each state, so an assistant still producing output at four in the morning reads as moving now, and a session somebody replied to ten minutes ago and which has gone quiet reads as ten minutes old and sits above one that stopped at midnight. Absent on a row no reading has answered for.",
      "type": "object",
      "additionalProperties": false,
      "required": ["known"],
      "properties": {
        "at": { "description": "Unix seconds: when the record last grew. Present only when `known`.", "type": "integer" },
        "detail": { "description": "This machine's own sentence about the answer, for a reader deciding whether to wait, fix this machine or stop asking.", "type": "string" },
        "evidence": { "$ref": "common.schema.json#/$defs/Evidence" },
        "known": { "description": "Whether there is a time. False is a real answer and carries `unknown_reason`: a client must draw it as unknown and order it ahead of every known time inside its state, never as a very old time. Of the two ways to be wrong about a row nobody could read, only `it has been quiet for a week` hides the row somebody has to act on.", "type": "boolean" },
        "unknown_reason": { "$ref": "#/$defs/ActivityUnknownReason" }
      }
    },
    "SessionRow": {
      "description": "One assistant session. Fields that cannot be supported are absent rather than invented; a reader that handles absence handles this too.",
      "type": "object",
      "additionalProperties": false,
      "required": ["backend", "closeability", "evidence", "id", "isClaude", "state", "work_state"],
      "properties": {
        "activity": { "$ref": "#/$defs/SessionActivity" },
		"agents": { "description": "Provider-native background threads, newest running work first. Broker children stay in the task list and are joined with these in the console, so their durable state is not flattened into a file inference.", "type": "array", "items": { "$ref": "#/$defs/SessionAgent" } },
		"agents_reading": { "$ref": "#/$defs/SessionAgentReading" },
        "assistant": { "$ref": "common.schema.json#/$defs/Assistant" },
        "backend": { "$ref": "common.schema.json#/$defs/Backend" },
        "closeability": { "$ref": "#/$defs/Closeability" },
        "coordination": { "$ref": "#/$defs/SessionCoordination" },
        "coordinator": { "$ref": "#/$defs/SessionCoordinator" },
        "cwd": { "type": "string" },
        "disposition": { "$ref": "#/$defs/WorkDisposition" },
        "evidence": { "$ref": "common.schema.json#/$defs/Evidence" },
        "icon": { "$ref": "common.schema.json#/$defs/Icon" },
        "identity": { "$ref": "#/$defs/IdentityBinding" },
        "id": { "type": "string" },
        "isClaude": { "type": "boolean" },
        "label": { "description": "What the session is called, by the Swift app's rungs (ITerm.swift preferredDisplayLabel): a name typed in Clawdline (the Swift store's config.json `session_titles`), the title of the task, handoff or Feature Root Clawdline opened the tab for (orchestrator.json), the conversation's own title (`/rename`, else `aiTitle`), Codex's thread name or an automatic title, Claude Code's registry handle — the first that has something. Never a terminal title. Absent when nothing names the session.", "type": "string" },
        "line": { "description": "What a working session says it is doing, read from its screen with the assistant's own clock in it. Present while working; on a waiting session with a menu it is instead that menu's revision (the Swift app's menuRevision), which no row draws and which changes whenever the question does. The Swift app sends it under this name and the row's height depends on it.", "type": "string" },
        "menu": { "$ref": "#/$defs/SessionMenu" },
        "owed": { "$ref": "#/$defs/WorkOwed" },
        "root_assignment": { "$ref": "#/$defs/RootAssignmentRecord" },
        "source": {
          "description": "When and how this row's terminal state was read. `current` was read this pass; `unverified` is the last complete row retained across a failed source reading; `missing` means that source has never answered or its retained answer crossed the cache.session_inventory honesty line. A retained state is a true statement about that earlier moment, never authority for an action now.",
          "$ref": "coordination.schema.json#/$defs/BearingsSource"
        },
        "sessionId": { "description": "The assistant's own conversation id, when one was recovered from its command line.", "type": "string" },
        "shells": { "description": "The commands this session left running in the background, newest first, at most six. Absent rather than empty when there are none, as the Swift app sends it; always absent for Codex, which keeps no record of them.", "type": "array", "items": { "$ref": "#/$defs/SessionShell" } },
        "state": { "$ref": "common.schema.json#/$defs/SessionState" },
        "tty": { "type": "string" },
        "work_moved_by": { "description": "Who the declaring session said will move it — a session id, or a person.", "type": "string" },
        "work_note": { "description": "The session's own words for its state, present only when its declaration decided `work_state` (then `work_provenance` is `self`).", "type": "string" },
        "work_person_needed": { "type": "boolean", "description": "Whether that mover is a person. Absent unless the declaration decided the state; present and false is a real answer." },
        "work_provenance": { "$ref": "#/$defs/WorkProvenance" },
        "work_since": { "description": "Unix seconds: when that declaration was made.", "type": "integer" },
        "work_state": { "$ref": "#/$defs/WorkState" }
      }
    },
	"SessionAgentState": {
	  "description": "A shared presentation state. `unknown` means the source could not establish an ending; it is never idle. Broker tasks keep their original state beside this projection.",
	  "type": "string",
	  "enum": ["running", "done", "failed", "unknown"]
	},
	"SessionAgent": {
	  "description": "One provider-native background thread. `what` and `type` are provider words; neither is inferred from an id.",
	  "type": "object",
	  "additionalProperties": false,
	  "required": ["at", "depth", "id", "state", "type", "what"],
	  "properties": {
		"at": { "description": "Unix seconds; zero when the provider did not expose one.", "type": "integer" },
		"depth": { "type": "integer" },
		"doing": { "type": "string" },
		"id": { "type": "string" },
		"model": { "type": "string" },
		"result": { "type": "string" },
		"seconds": { "type": "number" },
		"state": { "$ref": "#/$defs/SessionAgentState" },
		"tokens": { "type": "integer" },
		"tools": { "type": "integer" },
		"type": { "type": "string" },
		"what": { "type": "string" }
	  }
	},
	"SessionAgentReadState": {
	  "description": "`complete` makes an empty list authoritative. `unknown` makes it no count at all.",
	  "type": "string",
	  "enum": ["complete", "unknown"]
	},
	"SessionAgentUnknownReason": {
	  "description": "Why provider-native child work could not be counted. The console translates this instead of displaying adapter prose.",
	  "type": "string",
	  "enum": ["no_record", "unreadable", "unrecognized"]
	},
	"SessionAgentReading": {
	  "description": "The evidence state accompanying `agents`. Omitted on an older daemon is unknown, never complete empty.",
	  "type": "object",
	  "additionalProperties": false,
	  "required": ["state"],
	  "properties": {
		"reason": { "$ref": "#/$defs/SessionAgentUnknownReason" },
		"state": { "$ref": "#/$defs/SessionAgentReadState" },
		"truncated": { "description": "Known rows omitted by the per-session presentation bound.", "type": "integer" }
	  }
	},
    "SessionMenu": {
      "description": "The question on a waiting session's screen, as rows a finger can hit (the Swift app's menuObject). Present only on a waiting session whose screen could be read as a menu; a waiting row without it is a question drawn in a shape nothing recognised. The row labels and details may be refilled from the session's own transcript, but only once the screen proves which question it is showing; the numbers, the caret, Submit and the steps are always the screen's.",
      "type": "object",
      "additionalProperties": false,
      "required": ["options"],
      "properties": {
        "options": { "type": "array", "items": { "$ref": "#/$defs/SessionMenuOption" } },
        "question": { "description": "The prose above the rows, or the question as the call asked it. Absent when it could not be read.", "type": "string" },
        "selected": { "description": "The number of the row the caret is on. Absent when it is on none of them — a multi-select's button can hold it.", "type": "integer" },
        "steps": { "description": "Where this question sits in a set of them, as the picker's tab bar draws it. Absent for a lone question.", "type": "array", "items": { "$ref": "#/$defs/SessionMenuStep" } },
        "submit": { "$ref": "#/$defs/SessionMenuSubmit" }
      }
    },
    "SessionMenuOption": {
      "description": "One row. `n` is the keystroke and not the position: the page sends that number to `POST /v1/sessions/{id}/key`, and renumbering would make a button answer a different question than its label says.",
      "type": "object",
      "additionalProperties": false,
      "required": ["can", "label", "n", "selected"],
      "properties": {
        "can": { "description": "Whether a keystroke reaches this row (1…9). A row that cannot be answered is drawn and not offered.", "type": "boolean" },
        "checked": { "description": "Only on a multi-select, where a row ticks rather than answers; present and false is a real answer (the daemon writes it through its own wire type, since a generated optional bool would drop false).", "type": "boolean" },
        "detail": { "description": "The prose under the label, joined.", "type": "string" },
        "label": { "type": "string" },
        "n": { "type": "integer" },
        "selected": { "description": "The caret is on this row, so it is what a bare Return would confirm.", "type": "boolean" }
      }
    },
    "SessionMenuSubmit": {
      "description": "The button under a multi-select's rows. It has no number on screen; `POST /key` takes the word `submit` for it.",
      "type": "object",
      "additionalProperties": false,
      "required": ["label", "selected"],
      "properties": {
        "label": { "type": "string" },
        "selected": { "type": "boolean" }
      }
    },
    "SessionMenuStep": {
      "description": "One question of a set, as the picker's tab bar names it.",
      "type": "object",
      "additionalProperties": false,
      "required": ["done", "label"],
      "properties": {
        "answer": { "description": "What was chosen, once the picker's review screen names it.", "type": "string" },
        "done": { "type": "boolean" },
        "label": { "type": "string" }
      }
    },
    "SessionShell": {
      "description": "One background command a session started and has not finished: its output file has no ending written under it, and the session's transcript announced its id as a background command. `at` is when it last printed something.",
      "type": "object",
      "additionalProperties": false,
      "required": ["at", "id"],
      "properties": {
        "at": { "description": "Unix seconds.", "type": "integer" },
        "command": { "description": "The command line that started it, on one line. Absent when the two transcript records it is joined from straddled a read.", "type": "string" },
        "doing": { "description": "The last line it printed, clipped to 160 characters.", "type": "string" },
        "id": { "description": "Claude Code's own id for it.", "type": "string" },
        "what": { "description": "The description written beside the command, when there was one.", "type": "string" }
      }
    },
    "SessionModel": {
      "description": "One model this session's assistant can be switched to. A session's current model is matched against `id` by prefix, so a dated id still finds its row; `name` is what a reader is shown for it.",
      "type": "object",
      "additionalProperties": false,
      "required": ["command", "id", "name"],
      "properties": {
        "command": { "type": "string" },
        "id": { "type": "string" },
        "name": { "type": "string" }
      }
    },
    "SessionInfoSession": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id"],
      "properties": {
        "assistant": { "$ref": "common.schema.json#/$defs/Assistant" },
        "cwd": { "type": "string" },
        "id": { "type": "string" },
        "model": { "description": "The model the session is on, as its record last named it: the newest assistant turn's model, or a `/model` switch made after it. Absent when the record says nothing.", "type": "string" },
        "sessionId": { "type": "string" },
        "title": { "description": "The same label the session list shows.", "type": "string" }
      }
    },
    "SessionInfoUsage": {
      "description": "What the conversation has spent. For Claude it is the sum of every assistant turn in the transcript and is absent when the transcript is larger than 8 MiB, because a partial sum is not a total. For Codex it is Codex's own running total.",
      "type": "object",
      "additionalProperties": false,
      "required": ["cacheRead", "cacheWrite", "input", "output", "total"],
      "properties": {
        "cacheRead": { "type": "integer" },
        "cacheWrite": { "type": "integer" },
        "costUsd": { "description": "US dollars. Claude Code's own session total from its status-line cache when it wrote one, otherwise these tokens at list price. Absent when neither is known, which is always the case for Codex; also absent at exactly zero.", "type": "number" },
        "input": { "type": "integer" },
        "model": { "description": "The model the last counted turn named, as written.", "type": "string" },
        "output": { "type": "integer" },
        "total": { "type": "integer" }
      }
    },
    "SessionInfoContext": {
      "description": "How full this conversation's context window is. The used side is counted — Claude's newest parent assistant turn, or Codex's own last turn — and survives a transcript too large to total, because the newest turn is at the end of the file. Absent when nothing said how much fits: a Claude session on a model with no window row and no status-line cache draws no reading at all, because unknown is not 0%.",
      "type": "object",
      "additionalProperties": false,
      "required": ["usedPercent"],
      "properties": {
        "usedPercent": { "description": "0\u2026100, clamped: a conversation past its window is full, not 104% full.", "type": "number" },
        "usedTokens": { "description": "The counted used side. Absent when the only reading available was a percentage somebody else worked out; also absent at exactly zero, which `usedPercent` still states.", "type": "integer" },
        "windowTokens": { "description": "How much fits \u2014 **only when the assistant itself said so** (Claude Code's `context_window_size`, Codex's `model_context_window`). A window this daemon guessed from the model id does not go on the wire, while `usedPercent` still does: a percentage a client draws as one number is read as an approximation, and `162,277 / 1,000,000 tokens` in a tooltip is read as a measurement \u2014 only one of those two survives being wrong.", "type": "integer" }
      }
    },
    "SessionLimitWindow": {
      "description": "One plan window, as the provider last described it.",
      "type": "object",
      "additionalProperties": false,
      "required": ["hit", "name", "usedPercent"],
      "properties": {
        "hit": { "description": "The window is spent: the provider refused a request on it, or reported 100% or more.", "type": "boolean" },
        "name": { "description": "`5h`, `7d`, or the window's length for any other (`1d`, `90m`).", "type": "string" },
        "resetsAt": { "description": "Unix seconds. Absent when the provider did not say.", "type": "integer" },
        "usedPercent": { "description": "As the provider reported it, unrounded; it can exceed 100.", "type": "number" }
      }
    },
    "SessionLimits": {
      "description": "The plan windows of the account this session's assistant runs on: an account-level reading shared by every session of that assistant (the Swift app's `AssistantQuota.machineLimits`), not this conversation's. An empty `windows` is \"nobody said\", never 0%, and `unknownReason` says which kind of nothing it was. It is the same reading GET /v1/orchestrator/assistants answers with, age and all, so the two can never show one account two ways.",
      "type": "object",
      "additionalProperties": false,
      "required": ["windows"],
      "properties": {
        "at": { "description": "Unix seconds of the provider record the windows came from. Absent when nothing has been read.", "type": "integer" },
        "readAtMs": { "description": "Unix milliseconds when this daemon took the reading. A client holding readings from several answers keeps the newest by this. Absent for a session with no assistant.", "type": "integer" },
        "ageSeconds": { "description": "How old the provider's record is, in seconds, at the moment this answer was made. Absent when nothing has been read. A percentage is drawn beside its age or it reads as a number from now.", "type": "integer", "nullable": true },
        "stale": { "description": "The reading is older than `freshForSeconds`, so these percentages are a lower bound and not a current reading.", "type": "boolean" },
        "freshForSeconds": { "description": "How long this reading was good for: five percent of its shortest live window, never under fifteen minutes and never over six hours. Null when there is no reading to age.", "type": "integer", "nullable": true },
        "detail": { "description": "The same sentence GET /v1/orchestrator/assistants puts in `detail`: what the windows say and how old they are, or which kind of nothing an empty reading was.", "type": "string" },
        "unknownReason": { "description": "Why there is no reading, when there is none. Absent when windows were read.", "$ref": "orchestrator.schema.json#/$defs/AssistantUnknownReason" },
        "windows": { "type": "array", "items": { "$ref": "#/$defs/SessionLimitWindow" } }
      }
    },
    "SessionInfo": {
      "description": "The facts behind the status line under an open session. This daemon serves the transcript-derived part the Swift app calls the summary, the context reading, the plan windows and the project's addresses; the working tree, permission and fast mode are not read here and their keys are absent.",
      "type": "object",
      "additionalProperties": false,
      "required": ["models", "session"],
      "properties": {
        "context": { "$ref": "#/$defs/SessionInfoContext" },
        "limits": { "$ref": "#/$defs/SessionLimits" },
        "models": { "type": "array", "items": { "$ref": "#/$defs/SessionModel" } },
        "session": { "$ref": "#/$defs/SessionInfoSession" },
        "usage": { "$ref": "#/$defs/SessionInfoUsage" },
        "links": {
          "description": "Where this project can be opened, the same rows GET /v1/sessions/{id}/links answers with (links.schema.json). Served from the same maintained projection, so the card and the sheet cannot show one project two ways. Absent when the session has no working directory.",
          "type": "array",
          "items": { "$ref": "links.schema.json#/$defs/ProjectLink" }
        },
        "deploy": {
          "description": "The `links` rows whose kind is `deploy`, `ci` or `run`, unchanged, so a state means there what it means there. It is the smaller field the Swift app kept for older clients, and the one the status line's chip is drawn from.",
          "type": "array",
          "items": { "$ref": "links.schema.json#/$defs/ProjectLink" }
        },
        "linksObservedAt": {
          "description": "When the walk behind `links` was taken, in seconds since 1970. A held reading is served however old it is, so a card that wants to say how old it is has the number.",
          "type": "number"
        },
        "repository": {
          "description": "What the one git read behind `links` found. Present whenever the session has a working directory, including when it produced no row: `could not read` and `there is none` are different sentences.",
          "$ref": "links.schema.json#/$defs/ProjectRepository"
        },
        "repositoryUnreadable": { "$ref": "links.schema.json#/$defs/ProjectGitFailure" },
        "deployQuiet": {
          "description": "Why a repository that *is* on GitHub still has no deploy row here. `repository: github` says a workflow run could have been named; this says what was found under that name and, where the file gave one, the producer's own reason for having nothing to show. Without it a project with a wedged poller and a project between runs are one blank cell.",
          "$ref": "links.schema.json#/$defs/ProjectDeployQuiet"
        }
      }
    },
    "SessionInfoReply": {
      "description": "GET /v1/sessions/{id}/info. Kept off the session stream because answering reads the session's record; the console holds an answer for a minute.",
      "type": "object",
      "additionalProperties": false,
      "required": ["info"],
      "properties": {
        "info": { "$ref": "#/$defs/SessionInfo" }
      }
    },
    "SessionTitleRequest": {
      "description": "POST /v1/sessions/{id}/title. A visible one-line name; an empty or whitespace-only string clears the name this Clawdline installation keeps.",
      "type": "object",
      "additionalProperties": false,
      "required": ["title"],
      "properties": {
        "title": { "type": "string" }
      }
    },
    "SessionTitleReply": {
      "description": "The durable local result of naming a session. This daemon keeps the display name in its own config and does not mutate the assistant's conversation metadata.",
      "type": "object",
      "additionalProperties": false,
      "required": ["display_title", "downstream", "downstream_synced", "local_applied", "ok", "title"],
      "properties": {
        "display_title": { "description": "The label the session list and Session Info show after this write, including the fallback revealed by clearing a local title.", "type": "string" },
        "downstream": { "description": "local_only in this daemon; retained as a string because older machines may report a downstream assistant sync state.", "type": "string" },
        "downstream_synced": { "type": "boolean" },
        "local_applied": { "type": "boolean" },
        "ok": { "type": "boolean" },
        "title": { "description": "The normalized local title, or empty when it was cleared.", "type": "string" }
      }
    },
    "ScanCompleted": {
      "type": "object",
      "additionalProperties": false,
      "required": ["complete", "sequence"],
      "properties": {
        "complete": { "type": "boolean" },
        "sequence": { "type": "integer" }
      }
    },
    "ScanGap": {
      "description": "One region of a source that this reading could not see into, named. A source that says only `incomplete` has told a reader that its list is short and nothing else, so every absence on the machine becomes unprovable at once \u2014 including absences that source was never asked about. Naming the region lets a reader ask the narrower question instead: could what I am looking for be in the part that was not read?",
      "type": "object",
      "additionalProperties": false,
      "required": ["scope", "detail", "sealed"],
      "properties": {
        "scope": { "description": "The kind of region, in the source's own words: `window`.", "type": "string" },
        "id": { "description": "The region's own id, where the source can name one. Absent when it could not, and a region with no name is never sealed.", "type": "string" },
        "detail": { "description": "What was asked and what came back, in one sentence, for a person reading it.", "type": "string" },
        "sealed": { "description": "Another source has accounted for everything this region could be hiding, so it no longer costs its source its authority. Time never sets this: a window unread for two hours is a window unread, and `long enough` is the same guess made more slowly.", "type": "boolean" },
        "sealedBy": { "description": "That reason with its numbers in it, so a reader can check it rather than take it. Absent on a region that is still open.", "type": "string" }
      }
    },
    "ScanSource": {
      "description": "One source's own answer about itself. The merged `complete` is the AND of these, which is the right answer to `is this list all there is` and the wrong one to `is this pane gone`: only the source that reads panes can say that. A reader that must decide whether a session it remembers is really absent asks the source that would have seen it.",
      "type": "object",
      "additionalProperties": false,
      "required": ["complete", "source"],
      "properties": {
        "complete": { "type": "boolean" },
        "source": { "description": "The source's provenance, as the adapter names it: `ps`, `tmux`, `iterm`.", "type": "string" },
        "gaps": { "description": "The regions of this source that could not be read, each one either sealed by another source or still open. Optional: a source that read everything omits it. A person whose machine has gone quiet reads this to find out what is in the way.", "type": "array", "items": { "$ref": "#/$defs/ScanGap" } }
      }
    },
    "Scan": {
      "description": "What the reading behind this snapshot knows about itself. Without it an empty list cannot be told apart from a failed scan.",
      "type": "object",
      "additionalProperties": false,
      "required": ["complete", "completed", "emptyAuthoritative", "epoch", "generation", "provenance"],
      "properties": {
        "complete": { "type": "boolean" },
        "completed": { "$ref": "#/$defs/ScanCompleted" },
        "emptyAuthoritative": { "description": "An empty list is only authoritative when the reading was complete. This is what stops a client reading a failed scan as `every session went away`.", "type": "boolean" },
        "epoch": { "description": "Identifies the process. A client reconnecting to a restarted daemon uses it to tell a reset generation from one that went backwards.", "type": "integer" },
        "generation": { "type": "integer" },
        "notes": { "description": "The terminal adapters' reasons this reading did not finish. Optional on a complete reading. These diagnostic details cross the wire so a client can translate the named failure into an action instead of drawing an unexplained empty list.", "type": "array", "items": { "type": "string" } },
        "provenance": { "type": "string" },
        "source": {
          "description": "What the whole displayed batch is worth. `unverified` means at least one terminal source failed this pass and rows from its last complete reading were retained; `missing` means at least one failed source had no retained reading inside cache.session_inventory. Per-row source says which rows are current.",
          "$ref": "coordination.schema.json#/$defs/BearingsSource"
        },
        "sources": { "description": "Each source's own completeness, by provenance name, in a stable order. Optional: a reading with no per-source answer omits it, and a client written before it existed is unaffected.", "type": "array", "items": { "$ref": "#/$defs/ScanSource" } }
      }
    },
    "SessionsSnapshot": {
      "type": "object",
      "additionalProperties": false,
      "required": ["at", "scan", "sessions"],
      "properties": {
        "at": { "type": "integer" },
        "scan": { "$ref": "#/$defs/Scan" },
        "sessions": { "type": "array", "items": { "$ref": "#/$defs/SessionRow" } }
      }
    },
    "SessionCoordinatorCommand": {
      "description": "One command in the Clawdfather panel. The list is the Swift app's fixed table (Coordinator.swift `commands`), in its order. `reason` and `why` are present only when `enabled` is false.",
      "type": "object",
      "additionalProperties": false,
      "required": ["enabled", "token_effort", "token_effort_basis", "type"],
      "properties": {
        "enabled": { "type": "boolean" },
        "reason": { "type": "string" },
        "token_effort": { "type": "string" },
        "token_effort_basis": { "type": "string" },
        "type": { "type": "string" },
        "why": { "type": "string" }
      }
    },
    "SessionCoordinator": {
      "description": "Present on exactly the row whose live process is the one ~/.config/clawdline/coordinator.json registers — terminal, assistant, tty, pid, start time and conversation all equal. `status` is always `online` here, because the field is only attached to a matching live row.",
      "type": "object",
      "additionalProperties": false,
      "required": ["commands", "label", "status"],
      "properties": {
        "commands": { "type": "array", "items": { "$ref": "#/$defs/SessionCoordinatorCommand" } },
        "label": { "type": "string" },
        "status": { "type": "string" }
      }
    },
    "CoordinationWaitRow": {
      "description": "One file-ownership wait between sessions, from the Swift store's `coordination_waits`, as Orchestrator.coordination(forTerminal:) shapes it. On the waiting side it carries `reason` and `waiterCreatedAt`; on the owner's side `waiterSessionId` and `reason`. The label beside a session id is present when that session is on screen.",
      "type": "object",
      "additionalProperties": false,
      "required": ["createdAt", "id", "ownerSessionId", "paths", "releaseCondition", "repository"],
      "properties": {
        "createdAt": { "type": "integer" },
        "id": { "type": "string" },
        "ownerLabel": { "type": "string" },
        "ownerSessionId": { "type": "string" },
        "paths": { "type": "array", "items": { "type": "string" } },
        "reason": { "type": "string" },
        "releaseCondition": { "type": "string" },
        "repository": { "type": "string" },
        "waiterCreatedAt": { "type": "integer" },
        "waiterLabel": { "type": "string" },
        "waiterSessionId": { "type": "string" }
      }
    },
    "SessionCoordination": {
      "description": "Who this session is parked on, and who is parked on it. Absent when neither. `state` is `waiting_on_session` whenever `waitingOn` is not empty, otherwise `has_waiters`.",
      "type": "object",
      "additionalProperties": false,
      "required": ["state", "waitedOnBy", "waitingOn"],
      "properties": {
        "state": { "type": "string" },
        "waitedOnBy": { "type": "array", "items": { "$ref": "#/$defs/CoordinationWaitRow" } },
        "waitingOn": { "type": "array", "items": { "$ref": "#/$defs/CoordinationWaitRow" } }
      }
    },
    "WorkDisposition": {
      "description": "What a finished row delivered, present only with `milestone_complete` or `work_complete`. `scope` is `session` for a session's own delivery receipt and `task` for the task that opened it; the landing fields are present only when a broker-verified landing backs `work_complete`.",
      "type": "object",
      "additionalProperties": false,
      "required": ["evidence", "scope", "title"],
      "properties": {
        "commit": { "type": "string" },
        "evidence": { "type": "string" },
        "landedAt": { "type": "integer" },
        "receiptAt": { "type": "integer" },
        "scope": { "type": "string" },
        "target": { "type": "string" },
        "targetCommit": { "type": "string" },
        "taskId": { "type": "string" },
        "title": { "type": "string" }
      }
    },
    "WorkOwed": {
      "description": "Something this session declared it is owed, beside whatever its work state is.",
      "type": "object",
      "additionalProperties": false,
      "required": ["note", "person_needed", "provenance", "since"],
      "properties": {
        "moved_by": { "type": "string" },
        "note": { "type": "string" },
        "person_needed": { "type": "boolean" },
        "provenance": { "type": "string" },
        "since": { "type": "integer" }
      }
    },
    "RootAssignmentRecord": {
      "description": "A broker-proved independent Feature Root this session owns.",
      "type": "object",
      "additionalProperties": false,
      "required": ["explanation", "id", "label", "ownership", "state"],
      "properties": {
        "explanation": { "type": "string" },
        "id": { "type": "string" },
        "label": { "type": "string" },
        "ownership": { "type": "string" },
        "state": { "type": "string" }
      }
    }
  }
}
