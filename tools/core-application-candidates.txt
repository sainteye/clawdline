# Core/Application candidate manifest — W2-3 correction (F2).
#
# Every non-comment, non-blank path below is a file `tools/check-architecture-boundaries.sh`
# holds to the Core/Application boundary: it must not import AppKit, Security,
# ServiceManagement, Speech, AVFoundation or Carbon, and it must not spell a platform effect
# (iTerm2, tmux, the Mac facade, the pasteboard or workspace, the Keychain, a file manager, a
# subprocess, a signal, a sleep or the wall clock) on a code line. Sources/HostPorts.swift keeps
# its own stricter check too — imports Foundation and nothing else — because it is where the
# port contracts and the safe-close lifecycle live; that check is unchanged by this file.
#
# This manifest only grows. The guard reads its line count against a floor recorded beside the
# check; lowering the floor is a decision made in the same change that removes an entry, not a
# side effect of one going stale. Do not delete a line to make a shrunk candidate set pass —
# raise the floor next to a stated reason, or restore the entry.
#
# What this proves: that the listed files are lexically free of the platform spellings above, on
# this tree, right now. It does not prove they compile on Linux, and it is not a substitute for
# tools/ubuntu-core-probe.sh's own manifest.
#
# W3-1 added the three lines below and, in the same delivery, made all four files real members of
# the ClawdlineCore/ClawdlineApplication SwiftPM targets in Package.swift — Packages/README.md
# says why they are symlinks there rather than a second copy, and
# tools/check-architecture-boundaries.sh's "real SwiftPM Core/Application/Mac target graph" block
# checks that every real target member is also listed here, so a new target member cannot skip
# this scan. Sources/CloudCanonicalJSON.swift and Sources/CloudClock.swift already had a genuine
# (not lexical) Ubuntu 24.04 compile receipt from tools/ubuntu-core-probe.sh before this file
# named them; tools/swift-core-application-linux-build.sh now proves the same for the real
# targets themselves, under the same pinned image.
Sources/Assistant.swift
Sources/CloudCanonicalJSON.swift
Sources/CloudClock.swift
Sources/HostPorts.swift
