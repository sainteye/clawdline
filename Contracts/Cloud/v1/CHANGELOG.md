# Changelog

## 1.0.0 — 2026-09-10 (candidate only)

- Publish one manifest, five closed schemas, closed channel/class and evidence vocabularies.
- Derive exact valid/invalid bytes, signing inputs, source and package digests offline.
- Preserve six envelope known answers, ctlr request/reply-key proof, five receipt bodies and pairing_handover from the unchanged public legacy corpus.
- Declare package floor/range and wire range without raising deployed client floors.
- Record nonce example, ctlr reader and private pairing mirror gaps; old PROTOCOL prose authority remains until explicit cutover.
- No production, private mirror, consumer migration, app release or deployment is part of this candidate.

## Unreleased candidate corrections (2026-09-10)

- Replace large-ct repeated-group regexes with an alphabet filter plus mandatory base64
  round-trip/decoded-length validation for both envelope and grant wrapper.
- Add separate crypto-negative agreement/result expectations; preserve the legacy vectors object.
- Publish fixed-baseline compatibility gaps, measurement units, owners and pending acceptance.
- Type malformed regex/CLI failures, enforce exact Boolean vector metadata, and publish with
  native atomic no-replace semantics (unsupported platforms/filesystems fail closed).
- Candidate only: no production policy, wire major, app floor or authority cutover changed.
