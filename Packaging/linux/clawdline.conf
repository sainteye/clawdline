# Dedicated non-root service identity. Home is durable because provider authentication state lives
# there; the provider still receives only the W4-1 closed environment and Landlock/seccomp profile.
u clawdline - "Clawdline headless service" /var/lib/clawdline/home /usr/sbin/nologin
