# 平台能力矩陣：固定 Mac 現況與 Ubuntu MVP 接受契約

W0-A，2026-09-10。Mac source commit `6f4411f1365258d1e1b41b76c85dcaa1dcb6b88a`，tree
`75314e95a17d901362c0d998477205606b9add5d`；來源讀取時間 2026-09-10 13:19–14:10 UTC。
下表的「現況」是原始碼閱讀，**本次未啟動 provider、編譯 Swift、重啟 app 或驗證已部署版本**。
Ubuntu 每列都是 Plan v4 的 MVP 契約，狀態均為尚待實作／驗證；沒有因 Mac 存在相似功能就標成支援。

能力與 command/query 的方向見 [ADR 0001](adr/0001-platform-boundary-and-evidence.md)，
GCE 信任與 wire authority 見 [ADR 0002](adr/0002-cloud-authority-and-executor-trust.md)。

| 能力 | 固定 Mac 現況與證据 | Ubuntu 24.04 amd64 MVP 必須證明 | 後續責任／明確缺口 |
|---|---|---|---|
| install／package provenance | [Package.swift](../Package.swift) 是 compiler-owned Mac／Linux product graph；[build.sh](../build.sh) 在 machine lock 內編譯 arm64 SwiftPM release product、驗 copy digest、寫 version，並支援 ad-hoc／Developer ID signing。不能稱每個 build 都有 release signature | [linux-package.sh](../tools/linux-package.sh) 先把 caller-pinned key/provenance/signature/archive 以 descriptor/no-follow 複製到 0700 staging，再只用該 snapshot 驗簽、驗 digest、精確解壓；signed schema/protocol identity 必須逐欄等於同一 target binary 的 machine-readable contract。release payload 逐次重驗，`current`/`previous`/receipt 由 fsynced journal 轉換 | private-root contract 覆蓋 replacement、link/device/topology、payload tamper、stale lock、transition kill 與 schema-health rollback；PID-1 VM 仍是 typed external gate。Mac signing 不是 Ubuntu provenance |
| provider install／auth | [Assistant.swift](../Sources/Assistant.swift):267–279 以既有 Claude／Codex CLI 啟動、沒有 provider token 參數；`:305–316` 只看 home 目錄以判 installed，不能證明 binary 或登入成功 | 以真實 Codex／Claude 完成啟動與交談，provider auth 經 secret boundary；token 不入 argv／log／URL／artifact，失效／撤銷可診斷 | W4-1；現有 CloudKeyStoring 僅是 Cloud key seam，不能拿來宣稱 provider provisioning 完成 |
| project registration／containment | [OrchestratorDraft.swift](../Sources/OrchestratorDraft.swift):338–340 驗可用 absolute directory，`:422–447` 拒 claims 的 absolute／`..`；claims 是 reservation，不是 filesystem lock | durable disk 上 allowlisted project roots；拒 traversal、symlink escape、undeclared writes；說明由 OS／sandbox／broker 哪層執行，每層有反例 | W4-1；目前沒有從 claims 推得全面 write isolation 的證據 |
| HOME／cwd／env | [Codex.swift](../Sources/Codex.swift):19–29 依 config、CODEX_HOME、user home 選路徑；[Assistant.swift](../Sources/Assistant.swift):203–235 清除 identity variables，保留其餘 login-shell env；[StartPoints.swift](../Sources/StartPoints.swift):112–119 quoted cd | non-root uid/gid、umask 0077、durable/isolated HOME、cwd、env allowlist 明列；provider secret 與子 process 不越界；拒繼承他人的 session identity | W4-1；Mac 是 env denylist，不是 Linux allowlist；隔離 HOME 不能破壞 provider 登入 |
| PTY／process ownership | [Tmux.swift](../Sources/Tmux.swift):562–620 透過 login shell、`-c cwd`、send-keys；`:296` 仍依 `ITerm.assistantPIDs()`；[ITerm.swift](../Sources/ITerm.swift):1221–1234、1326–1338 用 ps/tty/ppid/pgid | tmux socket 只屬服務使用者；PTY 與 child PID／start identity／process group 的 ownership 可追溯；使用 Linux procfs/POSIX 證据，避免誤殺重用 PID／他人 pane | W4-1；目前 tmux code 存在不代表已從 iTerm／Mac 工具分離 |
| terminal lifecycle | 同上；send-keys 成功只表示送達 tty，不等於 shell／agent 已執行；mutation admission seam 現由 [TerminalCommandScheduler.swift](../Sources/TerminalCommandScheduler.swift):60–114 擁有（W2-1 從 RemoteServer.swift 移出，depth 8／per-channel 2／nested-inline reservation 與 restart-maintenance rejection/drain 都在這個獨立檔），`RemoteServer` 保留原有 facade 名稱 | create、send、observe、interrupt、resize（支持時）、close、enumerate 的真實 tmux contract；共享一個 admission owner，完整區分 accepted/executed/delivered/observed/acknowledged | W4-1；terminal admission owner 已在 W2-1 獨立成檔，不因 Linux adapter 再建第二條 terminal lane |
| task／result／receipts | [Orchestrator.swift](../Sources/Orchestrator.swift):1260 task root 在 `/tmp/.clawdline`；`:7905–7923` ingests result 並比 secret hash；`:10194/10293` load/save | W4-3 candidate 在既有 serialized Linux ingress 先持久化 accepted＋sealed command，再執行 effect；schema-2 seal 可原 byte replay，task secret 在 command existence 前 constant-time 驗證。`/var/lib/clawdline/tasks/authority.json` 保存 task/Session/project/claims、secret digest 與 result/ack receipt；result bytes 只存一份且 startup/replay/ack/close 皆重新驗 digest/count，失敗先建 recovery obligation | disposable fixture 覆蓋 restart exact replay、schema-2 succeeded/interrupted、incomplete inventory 與 result tamper/missing/link；Cloud/GCE/PID-1/real provider 尚未證明。package rollback canonical-first，legacy 只容許 service-UID-owned pre-migration authority |
| Board／Session reads | [RemoteServer.swift](../Sources/RemoteServer.swift) 的 authenticated route switch；[ProjectBoardHTTP.swift](../Sources/ProjectBoardHTTP.swift) 分開 capability、read selectors 與 write gate；[CloudAppBridge.swift](../Sources/CloudAppBridge.swift) 的 `CloudHeadlessRead` 是 closed typed vocabulary | W4-3 candidate 以 task authority 投影 Board（`canWrite=false`／`canManage=false`）並回 exact Session/task terminal snapshot；每次 read 綁 project＋Session＋task，read 不進 command ledger、不保存、不能升成 dispatch/write/cleanup | 這不是第二個 Board store，也不改 Mac Board consent/lifecycle。Cloud read parity 仍屬 W5；W2-2 worktree cleanup 仍只接受 machine mutation authority |
| document reads | [ProjectArtifact.swift](../Sources/ProjectArtifact.swift):227–245 處理 project/task artifact roots，266–289 檢相對 path、resolved containment、regular type／extension／size；140–150 明載 hard-link／TOCTOU 限制 | W4-3 candidate 只從 exact project/Session/task identity 選 computed project/task artifact root；relative md/markdown/txt 限 depth/path/list/walk/2 MiB。Content 與 list 都持有 root/child fd，以 `openat`＋`O_NOFOLLOW` 驗 service owner、regular、`st_nlink==1`；list 在 bounded candidate set 排序後取 200，確有省略才 truncated | fixture 已拒 traversal、symlink、hardlink、FIFO/device、oversize、root replacement，並覆蓋 200/201/depth/walk 與 read-to-write；不宣稱抵擋同 uid 惡意 writer，Cloud canonical document identity仍屬 W5 |
| Web Session selection／effect lifetime | UX-1 correction candidate 由 [selection.js](../Resources/web/app/js/session/selection.js) 單獨持有 machine＋terminal route＋可用時的 conversation UUID、selection/transport epoch 與 effect lane；Cloud 同 account/device/relay 的短效 token 輪替保留 open/effect，已知 UUID、machine、route 或 transport 真的更換才作廢。Terminal／Git／Shell／Snippets／UserMessages／keys 均取 immutable exact route；legacy `S.openId`／`S.selectedId` 只是 projection | Ubuntu／Cloud console 必須沿用同一 identity 與 replacement/enrichment 規則；missing→present UUID 只可在同 machine/route 連續，重複 bare id、跨 machine title/id 推論、stale settlement、較新 composer draft 清除都必須 fail closed | UX-1 focused Node／static 與 mutation receipt；仍需 landing root exact-tree 驗收。這不新增 auto-send、resume、cleanup 或權限，亦不把 browser selection 當成 server authorization |
| local HTTP／SSE reliability | R-2 candidate 以 [HTTPReliability.swift](../Sources/HTTPReliability.swift) 單一 owner 管 128 admitted connection、16 capacity-response connection、64 MiB aggregate retained request、15 s request-read、16 SSE、4 MiB/per-stream、16 MiB aggregate SSE、20 s completion 與 144 個可立即移除的 deadline job；aggregate pressure 淘汰實際最大慢 debt owner。[ReadingFreshness.swift](../Sources/ReadingFreshness.swift) 同 key 16、全域 256 parked waiter，disconnect 可取消。`RemoteServer` 保留 auth／route／wire adapter，health 暴露 current/peak/refusal/eviction 而不列 identity | Linux adapter 必須沿用同一 application contract：auth 先於敏感輸出、completion-aware output、慢 consumer 隔離、current snapshot reconnect realign、無假 replay/ACK；不得另建無界 ingress/output/timer lane | R-2 是 source candidate、未部署；所有數字是 `implementation_default_pending_w6`，不是實測 adequacy。R-1 Cloud ingress、W2-1 terminal scheduler 語意不由本列重定義；W6 以 reference workload/故障注入批准或改值 |
| Mac-only capability routing | AppKit/iTerm/Apple Events/voice/build code 存在；`capability_unavailable` 的 Swift 文字搜尋自 W2-3 delivery（commit `491a23aa837a7fcf7e335b5effb7a192aeb219a6`）起已有命中：[HostPorts.swift](../Sources/HostPorts.swift) 的 `HostCapabilityUnavailable.code`。Safe-close 在缺少 terminal/process capability 時於 effect 前拒絕；`Targets` 的 send/capture/reveal/answer byte channel 與 `StartPoints` 的 admitted create 也已把平台 effect 委派給 `TerminalHost`。Route admission、menu parsing 和 AppKit pasteboard policy 仍留在 Mac/application facade。**這仍只是這棵 macOS tree 的 lexical/behavior boundary，不是 Linux runtime 或 Ubuntu MVP 已支援的證明** | discover 支援能力；Xcode、iTerm2、Apple Events、voice、Mac-only build 在 side effect 前回 typed `capability_unavailable`，或只依明文批准 policy 路由到指定 host | W4-1 明確接手 start-route policy 與 `Targets.answer` menu parser／allowlist，抽入 `ClawdlineApplication` 並以 Mac/Linux 共用 fixtures、拒絕碼及 attach affordance 驗收；不靜默缺欄、不假裝成功、不擅自轉到另一台 Mac。具體 wire schema 仍由後續相容契約固定 |
| Cloud pairing／secret／replay | [CloudAccount.swift](../Sources/CloudAccount.swift) 實作 identity start、四相 phase write/poll、machine rotation 的 exact canonical routes；viewer 寫 offer/activate，machine 寫 grant/confirm，confirm receipt 後才 pin。`CloudExecutorIdentityAuthority` 是 Mac/Ubuntu 共用 protected owner；W5-1 ledger/spool 仍單獨擁有 effect identity、sequence、exact frame 與 settlement | Ubuntu protected-file adapter 執行同一 owner；protected leaf/dir 會拒絕 link、非 regular、錯 uid/gid、group/other mode 或 `nlink != 1`。每次 initial/reconnect 在 ledger/spool 開啟後重讀 exact identity/generation/key/revocation epochs | 公私 byte-identical 843-byte route-role vector SHA-256 `2f79369d4ee866976c6da2a41358c1aab5321ab0e6054bf8a3afe16e215f69a9`；public lifecycle extension `79504ce608fd278cecdb2e26f62d6d1c7e915ef66f94a79cc12ff240a95e410e` 固定 exact members、duplicate replay 與 confirm pin timing。三-call 路徑只相容舊 handover；W0-E 仍 candidate，無 cutover/floor/deploy。live Relay/GCE、real disk-full/PID-1/restore 仍待 W5-3/W6 |
| daemon restart | [SessionWatch.swift](../Sources/SessionWatch.swift):1179–1184 要 ready 且 outstanding 全零，1189–1203 以 queued-secret recoverability 判 blocker；spawning 一律阻擋，queued sealed secret 能解且 hash 相符可恢復；1207–1233 replacement 先 reconcile | W4-2 production loopback ingress 只有一個 serialized owner：effect 前保存 accepted＋sealed bytes/digest，回覆前保存 stage/response；startup transition 每 daemon epoch 一次，periodic observation 不重播；admission 等 authoritative startup 完成才開 | deterministic create→send→restart-classify→explicit matching recovery→close 與 boundary-kill fixtures已覆蓋；真 PID-1 daemon restart後同 pane/socket仍可達尚待 VM gate |
| VM reboot／unknown outcome | [SessionWatch.swift](../Sources/SessionWatch.swift):1015–1024 loss/restart grace 與 incomplete inventory 不判 lost；這是 Mac source policy | accepted-only 記 interrupted；未決 effect 不被猜成 success。incomplete inventory不判 pane missing；corrupt/future/semantic-invalid state 先 fsync recovery obligation再 quarantine，普通 tick/restart不能初始化空 authority | deterministic state/ledger failure injection已覆蓋 source invariant；W6仍需 VM power/reboot時序與 Plan v4 p95/120s 實測批准 |
| upgrade／backup／restore | Mac build/sign 路徑不證明跨 schema backup/restore；本次沒讀 live snapshots | install/rollback以 descriptor讀 state schema/minimum-reader；migration保留 reader floor。release tree、link pair、journal、receipt與 parent directories fsync；stale owner/phase可驗並回復到完整 old 或 new pair，image rollback不倒退 DB | deterministic interruption points與真正回報 durable schema 1 的 target health rollback已覆蓋；非空 backup/restore、epoch/sequence跨還原與 RPO/RTO仍屬 W5-4/W6 VM drill |
| GCE operations／health | [docs/cloud.md](cloud.md):3–6 記 separate private API/relay；本次 source inventory 不包含 live health、IAM、服務部署 | W4-2 protected health snapshot 有 exact package digest/build/source/config/durable-schema/protocol 與 reconcile counts，並分離 serviceReady 和未證 provider auth 的 ready；loopback-only config 不等於 GCE network/IAM | W5-3 獨立 GCE IaC/state、PID-1/unit、OS Login/IAP 與 monitoring；W6 故障驗證。Ubuntu executor 加入 plaintext 信任範圍，見 trust doc |

## 怎麼使用這張表

W0-F 的首個證據只應將「非空 Core 可以在指定 Ubuntu/toolchain compile」補實，不能一次把
全表升為支援。每個 runtime capability 的接受紀錄至少要包含 exact source tree、package／
build identity、OS/architecture、command、成功與拒絕情境、check count、duration、outcome。
restart／reboot／restore 需另列 interruption point 與 durable state，健康 endpoint 本身不足以證明。

技術後續：W0-E 等 W0-A/W0-D；W0-F 等 W0-A/W0-B；root 再進獨立 W0 review、correction、
exact verification、landing。本任務沒有待使用者決策；後續 policy／budget 的批准由 CLA-296
root 明確處理，不能把本表當成已取得批准。更完整的來源檔案與文字盤點見
[generated inventory](generated/platform-architecture-inventory.md)。
