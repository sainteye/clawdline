package capacity

// DeployHealthLimit is the registered row read by the Linux release tool.
// Keeping the name here lets the bound guard prove that the shell deploy's
// deadline has one source in the same register as every daemon bound.
const DeployHealthLimit = DeployHealthSeconds
