Read `AGENTS.md` before touching anything here. It says what this repository is, what the other
Clawdline checkout is and why it must not be modified, where work happens, and which checks must be
green before a commit.

The short version: **this is the product**; `~/code/clawdline` is the retired Swift app, kept
read-only as the record of what still has to migrate and how it was done the first time. Work in a
disposable worktree, never in this shared checkout. This repository is public — nothing of the
person's belongs in it.

Where instruction files conflict, the nearest domain file wins, and a task brief wins over all.
