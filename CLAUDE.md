Read `AGENTS.md` before touching anything here. It says what this repository is, where work
happens, and which checks must be green before a commit.

The short version: **this is the product**, at `~/code/clawdline`; the retired Swift app is archived
under `~/code/clawdline-archive-20260922` and is never modified. Work in a disposable worktree,
never in this shared checkout. This repository is public — nothing of the person's belongs in it.

Where instruction files conflict, the nearest domain file wins, and a task brief wins over all.
